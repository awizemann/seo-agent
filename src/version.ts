/** Single source of truth for the agent version — used in the MCP serverInfo
 * handshake and the crawl / AEO fetch user agents. Bump in lockstep with
 * package.json. */
export const VERSION = '1.6.0';
